"""
1. to use % symbol in print statement use formatted string f"" for that print statement

2. Loops- repeating/iterating something over&nover until a particular condition is satisfied

3. In for loop, when we use "range(x,y,+z)" or "range(x,y,-z)" here x is included but y 
is excluded.
Values are printed from x to y-1. z denotes how much to increase. By default if we write
range(x,y) then z will be incremented by +1. we use + or - for incr/decr accordingly.
If we write only range(x), then values will be printed from 0 to x-1 by default.

4. break is a control statement is used to stop a loop
"""